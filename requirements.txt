# ── Dependências diretas ───────────────────────────────────────────
requests==2.33.0                # HTTP client
python-dotenv==1.2.2            # Leitura do .env (GarminReport)
garminconnect                   # Autenticação e API Garmin Connect (cyberjunky)

# ── Dependências transitivas (fixadas para reprodutibilidade) ──────
certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
urllib3==2.6.3
